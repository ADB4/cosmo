ollama>=0.5.0  # need the `think` kwarg on ollama.chat (added in 0.5.0)
chromadb>=0.4.0
pymupdf4llm
tiktoken
flask>=3.0.0
gunicorn>=23.0.0  # production WSGI server (Docker CMD)
PyJWT>=2.8.0      # optional Cloudflare Access JWT verification (COSMO_CF_ACCESS_*)
